import 'package:flutter/material.dart';

class HapusOrderScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Hapus Order")),
      body: Center(child: Text("Form untuk menghapus order.")),
    );
  }
}
